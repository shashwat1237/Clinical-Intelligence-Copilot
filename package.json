{
  "name": "clinical-intelligence-copilot",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "install:all": "npm install"
  },
  "devDependencies": {
    "turbo": "1.11.2",
    "typescript": "5.3.3",
    "@types/node": "20.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

